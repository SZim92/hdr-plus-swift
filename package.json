{
  "name": "hdr-plus-swift-docs",
  "version": "1.0.0",
  "description": "Documentation tools for HDR+ Swift",
  "private": true,
  "scripts": {
    "lint:md": "markdownlint '**/*.md' --ignore node_modules --config .markdownlint.json",
    "fix:md": "markdownlint --fix '**/*.md' --ignore node_modules --config .markdownlint.json"
  },
  "devDependencies": {
    "markdownlint": "^0.31.1",
    "markdownlint-cli": "^0.36.0"
  }
} 