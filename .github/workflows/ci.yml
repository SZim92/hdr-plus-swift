# DEPRECATED - This file has been split into multiple workflow files
# This file will be removed in a future update
# 
# Please use the following workflow files instead:
# - main.yml: Main build workflow
# - pr-validation.yml: PR validation
# - release.yml: Release process
# - security.yml: Security scanning
# - performance.yml: Performance benchmarking
# - documentation.yml: Documentation generation
# - maintenance.yml: Maintenance tasks
# - scheduled.yml: Scheduled tasks

name: DEPRECATED - Legacy CI

on:
  workflow_dispatch:  # Only manual triggering now

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow has been deprecated and split into multiple specialized workflows."
          echo "Please use the new workflows instead."
          exit 0 