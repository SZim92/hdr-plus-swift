name: DEPRECATED CI

# This workflow is deprecated and scheduled for removal
# Please use the specialized workflow files instead:
# - main.yml
# - pr-validation.yml
# - release.yml
# - security.yml
# - performance.yml
# - documentation.yml
# - maintenance.yml
# - scheduled.yml

on: 
  # This workflow is disabled and won't run
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is deprecated. Please use the specific workflow files instead.'
        required: true
        default: 'Understood'

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: echo "This workflow is deprecated. Please use the specialized workflow files instead." 