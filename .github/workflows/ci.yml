name: macOS CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Build and Run Tests
        run: |
          # Updated project file and scheme
          xcodebuild -project burstphoto.xcodeproj -scheme burstphotoApp -destination 'platform=macOS' clean test 